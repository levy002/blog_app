<%= link_to user_post_path(@user, id: post.id) do %>
  <section>
    <p>Post #<%= post.id %></p>
    <p><%= post.text %></p>
    <p>
        <span>
          Comments: <%= post.comments_counter || 0 %>,
          Likes: <%= post.likes_counter || 0 %>
        </span>
    </p>
  </section>
<% end %>